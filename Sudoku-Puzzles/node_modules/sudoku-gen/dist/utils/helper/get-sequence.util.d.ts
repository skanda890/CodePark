import { Layout } from '../../types/layout.type';
import { TokenMap } from '../../types/token-map.type';
export declare const getSequence: (layout: Layout, seedSequence: string, tokenMap: TokenMap) => string;
